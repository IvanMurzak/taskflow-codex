{
  "_comment": "Invariants asserted in skills/taskflow-execute/SKILL.md. Shared core + harness-specific entries. See check_execute_contract.py.",
  "required_phrases": {
    "merge default": "`--merge=on-green`",
    "role-and-task-path dispatch": "Read and follow role_file first:",
    "no task-body preload": "never load the task body",
    "parallel batch": "before waiting",
    "root repository": "`repo: \".\"` means the root",
    "sole board writer": "only you may edit it",
    "scheduler board fields": "`id`, `group`, `seq`, `repo`, and `base_branch`",
    "integration branch option": "`--integration-branch=auto|off|<name>`",
    "integration branch per repo": "one ref per landing repository",
    "integration branch verified": "Verify the host honoured the base",
    "integration branch PR target": "taskflow-pr-base=<integration-ref>",
    "integration branch single land": "open ONE PR",
    "integration final state": "integration-landings row",
    "integration archive gate": "integration-landings row is also `✅`",
    "submodule superproject landing": "superproject `.` whenever any task targets a submodule",
    "native engine": "`native` uses git worktrees",
    "Codex-native spawn": "Call Codex's native",
    "bundled implementer role": "references/roles/taskflow-implementer.md",
    "blocking agent wait": "blocking `wait_agent`",
    "model routing fast": "`fast` to `gpt-5.6-luna`",
    "model routing mid": "`mid` to `gpt-5.6-terra`",
    "model routing top": "`top` to `gpt-5.6-sol`",
    "pipeline stays in Codex": "current Codex session",
    "no external agent host": "another model or agent CLI",
    "manager is not driver": "never launch a process-based driver",
    "toolkit minimum": "pipeline --version >= 0.17.0",
    "solo batches": "forces each named task into a one-task batch",
    "failure stop": "dispatch nothing else",
    "parallel ceiling": "fixed ceiling of 8",
    "never integration conflict": "`--merge=never` together with an active integration branch"
  }
}
